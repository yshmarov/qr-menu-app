<h1>
  <i class="fa-solid fa-list"></i>
  <%= t(".title") %>
</h1>

<header>
  <nav>
    <% @menu_categories.each do |menu_category| %>
      <%= active_link_to menu_path(menu_category:) do %>
        <%= I18n.t("config.menu_categories.#{menu_category}") %>
      <% end %>
    <% end %>
    <%= link_to t(".all"), request.path if request.query_parameters.any? %>
  </nav>
</header>

<div id="menu" class="grid-cols-auto-fit">
  <% @products.each do |product| %>
    <%= render partial: "menu/product", locals: { product: product } %>
  <% end %>
</div>
